server:
  port: 9002

spring:
  application:
    name: user-service
  datasource:
    url: jdbc:postgresql://host.docker.internal:5432/digi_drive
    username: postgres
    password: postgres
  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true

eureka:
  client:
    service-url:
      defaultZone: http://localhost:8761/eureka
